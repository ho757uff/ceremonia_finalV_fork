 <div style="text-align: center;">

<h1>Ceci est la page pour créer un nouvelle album photo</h1>

  
 <%= form_for [@event, @album], url: event_albums_path(@event) do |form| %>
  <!-- Vos champs de formulaire ici -->
  <%= form.label :title %>
  <%= form.text_field :title %>

  <br><br>

  <%= form.label :description %>
  <%= form.text_area :description %>

  <br>
  <br>

  <%= form.submit "Create Album" %>
<% end %>

<%= link_to 'retour au show event', event_path(@event), class: 'btn btn-success'%>
 

</div>
    <div style="text-align: center;">


<br><br>

<h1> ceci est la page new du controller locations<h1>
    <h2> Il sert à créer un lieu de rencontre pour une partie de la cérémonie</h2>


    <div style="text-align: center;">

<h1>Vous Pouvez Faire Ici Votre Proposition D'Evénement</h1>

    <%= form_for @location do |f| %>
      <div class="form-group">
        <%= f.label :place %>
        <%= f.text_field :place, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :address %>
        <%= f.text_field :address, class: 'form-control' %>
      </div>

<div class="form-group">
  <%= f.fields_for :event_locations do |event_location_form| %>
    <%= event_location_form.label :event_id, "Select an Event" %>
    <%= event_location_form.collection_select :event_id, @events, :id, :title, {}, {class: 'form-control'} %>
    <%= event_location_form.label :date %>
    <%= event_location_form.datetime_field :date %>
    <%= event_location_form.label :description %>
    <%= event_location_form.text_area :description, class: 'form-control' %>
  <% end %>
</div>

      <div class="form-group">
        <%= f.submit 'Create Location', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
